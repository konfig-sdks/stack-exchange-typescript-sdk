import { StackExchange } from "./index";

describe("stack-exchange-typescript-sdk", () => {
    it("initialize client", async () => {
        const stackexchange = new StackExchange({
        });
    });
});
